## 1. 构建入口与配置

- [x] 1.1 评估服务器产物型后端、前端和网关 Dockerfile 是否可在无源码服务器环境中构建
- [x] 1.2 将服务器前端镜像构建流程调整为使用已上传的 `dist/` 静态产物
- [x] 1.3 新增服务器镜像构建脚本，读取 `deploy/.env` 中的 `BACKEND_IMAGE`、`FRONTEND_IMAGE` 和 `NGINX_IMAGE`
- [x] 1.4 在构建脚本中加入 Docker、Docker Compose、配置文件和镜像标签缺失检查

## 2. 服务器启动与更新流程

- [x] 2.1 补充服务器启动命令，使用 `docker compose --env-file deploy/.env -f deploy/docker-compose.yml up -d`
- [x] 2.2 补充重新构建并更新服务的流程，确保 Compose 使用新构建的本地镜像
- [x] 2.3 明确服务器 `.env` 中端口、数据库密码、镜像标签和域名配置的修改要求
- [x] 2.4 保持现有 Compose 服务关系，确认后端仍等待 MySQL 健康检查后启动

## 3. 文档与故障排查

- [x] 3.1 在 README 或部署文档中新增“服务器构建镜像并部署”章节
- [x] 3.2 记录等价手工构建命令，方便不使用脚本时逐条执行
- [x] 3.3 补充构建失败排查，包括 Maven/npm 依赖下载、网络代理和镜像源问题
- [x] 3.4 补充启动失败排查，包括端口冲突、镜像标签不一致、数据库连接失败和 Nginx 代理失败

## 4. 验证

- [ ] 4.1 执行后端镜像构建验证
- [ ] 4.2 执行前端镜像构建验证
- [ ] 4.3 执行网关 Nginx 镜像构建验证
- [ ] 4.4 执行 Compose 配置解析验证
- [x] 4.5 记录部署后验证命令，包括前端根路径、`/api/portfolio/projects` 和 Compose 日志检查
