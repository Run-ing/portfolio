## 1. 配置模板

- [x] 1.1 梳理本地部署需要的环境变量，覆盖数据库、后端端口、前端 API 基础路径、Nginx 端口和 `/apps/` 代理目标
- [x] 1.2 补充本地部署配置示例，确保示例不包含真实敏感凭据，并说明每个配置项的默认值和用途
- [x] 1.3 调整现有后端和前端配置示例，使 Docker Compose 部署和本机进程部署都能按环境变量覆盖

## 2. Docker Compose 与 Nginx

- [x] 2.1 检查并完善 `deploy/docker-compose.yml`，确保 MySQL、后端和前端服务的启动顺序、端口映射和环境变量满足本地部署要求
- [x] 2.2 检查并完善 Nginx 配置，确保 `/` 返回前端应用，`/api/` 代理到后端，`/apps/` 代理目标可配置或有明确替换说明
- [x] 2.3 增加停止和清理说明，区分保留数据库数据卷与重建初始化数据的操作

## 3. 部署文档

- [x] 3.1 在项目文档中新增本地部署章节，说明推荐的 Docker Compose 部署流程
- [x] 3.2 在项目文档中补充本机进程部署流程，说明如何分别启动数据库、后端和前端静态服务
- [x] 3.3 在文档中补充常见故障排查，覆盖端口占用、数据库连接失败、前端 API 地址错误和 `/apps/` 代理不可用

## 4. 部署验证

- [x] 4.1 增加或记录部署验证命令，检查本地首页是否返回成功 HTTP 状态
- [x] 4.2 增加或记录部署验证命令，检查 `/api/portfolio/projects` 是否返回成功 HTTP 状态和 JSON 响应
- [x] 4.3 验证失败时输出或文档化可检查项，包括容器状态、端口映射、数据库连接和 Nginx 代理配置
- [x] 4.4 执行本地部署相关的静态检查或可用命令，确认新增文档和配置与仓库现状一致
